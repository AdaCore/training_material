project Default is

   package Compiler is
      for Default_Switches ("ada") use ("-g", "-gnata", "-gnato13");
   end Compiler;

   package Prove is
      for Proof_Switches ( "Ada" ) use ("--mode=all", "--warnings=on");
   end Prove;

   for Main use ("atest.adb", "stest.adb");

end Default;

