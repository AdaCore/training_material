project Default is

   package Compiler is
      for Default_Switches ("ada") use ("-g");
   end Compiler;

   package Prove is
      for Proof_Switches ( "Ada" ) use ("--mode=all", "--warnings=on");
   end Prove;

   for Main use ("main.adb");

end Default;
