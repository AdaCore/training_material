with Ada.Text_IO; use Ada.Text_IO;
with Constants;
package body List is

   -- Create a list whose maximum size is specified in Constants package

   -- Create subprogram to add to list (print a message if the list is full)

   -- Create subprogram to remove an item from the list

   -- Create subprogram to print the list

   -- Create a function to return the current length of the list

end List;
