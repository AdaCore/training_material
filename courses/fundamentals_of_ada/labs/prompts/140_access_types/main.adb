with Ada.Text_IO;      use Ada.Text_IO;
with Password_Manager; use Password_Manager;
procedure Main is

begin
   --  For each account, set the password string and counter

   for Login in Login_T'Range loop
      null;
      --  Print login, password string, and counter
   end loop;
end Main;
