with Ada.Text_IO; use Ada.Text_IO;
-- "with" your message packages
procedure Main is
-- Declare your message object
begin
   -- Build a message
   -- Print message contents
   Put_Line ("Message contents");
   -- Modify the message
   -- Print the (modified) message contents
   Put_Line ("Modified message contents");
end Main;
