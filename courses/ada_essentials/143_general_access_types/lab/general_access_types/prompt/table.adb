with Ada.Text_IO; use Ada.Text_IO;

package body Table is

   procedure Initialize is
   begin
      null;
      -- Initialize the sorted table.
      -- Hint: You will need a record that stores the value of
      -- the dice roll plus a pointer to the location where
      -- the count for the dice roll is stored.
   end Initialize;

   procedure Print (Message : String) is
   begin
      null;
      --  Sort your table before printing
   end Print;

end Table;
